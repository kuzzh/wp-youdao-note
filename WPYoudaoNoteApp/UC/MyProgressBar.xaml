<UserControl x:Class="WPYoudaoNoteApp.UC.MyProgressBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="50">

    <Grid x:Name="LayoutRoot" Background="{x:Null}">
        <TextBlock x:Name="txtTip" Margin="0" TextWrapping="Wrap" Text="" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" />
        <ProgressBar x:Name="bar" Height="10" Margin="0" Foreground="White" IsIndeterminate="True" />
    </Grid>
</UserControl>